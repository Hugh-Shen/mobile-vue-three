export interface PopularDestinationType {
  img: string;
  area: string;
  city: string;
}

export interface LocalType {
  site: string;
  img: string;
  count: number | string;
  rate: number | string;
}
